This command has been absorbed. Run `/deliver XXXX` instead.

`/deliver XXXX` merges the worktree branch into main, removes the worktree, records learnings, and cleans up — everything `/merge` did, in one step.
