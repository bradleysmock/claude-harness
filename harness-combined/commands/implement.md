This command has been absorbed. Run `/build XXXX` instead.

`/build XXXX` creates the worktree, runs the spec engine with gate/repair loop, writes implementation to target files, and presents a diff — everything `/implement` did, in one step.

If you haven't written specs yet, run `/write-spec XXXX` first.
