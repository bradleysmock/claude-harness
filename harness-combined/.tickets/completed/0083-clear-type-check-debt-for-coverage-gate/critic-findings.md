# Critic Findings — 0083-clear-type-check-debt-for-coverage-gate

## Round 1 — 2026-09-11

**Active panels**: Core (always), Python (`**/*.py`, root `pyproject.toml`/`requirements.txt`), Testing (`**/tests/**`). `panel_detect.py` could not be executed (the critic has no Bash tool); `triggers.md` entries were dispositioned by reading. Candidates deferred: CI/CD — no root `Makefile`, `Dockerfile*`, `.pre-commit-config.yaml`, `.github/workflows/**`, or lock-file manifest exists; HTTP-API, Database, Identity, Cryptography, AI-LLM, Observability, Performance, Distributed, and all frontend panels — no matching glob, dependency, path keyword, or content pattern in the eight changed files. Nothing skipped.

Phase: code · Ticket: 0083-clear-type-check-debt-for-coverage-gate · Round: 1

Verdict: no BLOCKER or MAJOR findings. FR-1 through FR-9 each trace to an implementation site: `pyproject.toml:22` and `:29-31` (FR-1/FR-3), `requirements.txt:15` (FR-2), the keyword-only builders at `tests/test_0062_finding_key.py:9` and `tests/test_0067_incremental_scope.py:11` (FR-4), explicit kwargs plus the annotated `compute(root: Path, sha: str, timeout: int) -> set[str] | None` at `tests/test_regression_baseline_multilang.py:208-225` matching `gates/_baseline.py:175`'s `compute_fn` (FR-5), `tests/test_0078_autopilot_watch_cli.py:92` (FR-6), `tests/test_0031_pr_comments.py:314`/`:324`/`:372` (FR-7), `tests/test_0056_ticket_lock.py:23` (FR-8), `tests/test_0036_parallel_gate.py:36` (FR-9). On NFR-2, every changed site either preserves its assertion exactly or strengthens it in the direction the FR demands (`isinstance` where `str()` coercion would have masked a shape violation). No test is deleted, skipped, loosened, or newly suppressed; the two remaining `# type: ignore` comments in the touched files (`tests/test_0031_pr_comments.py:54` `[misc]`, `tests/test_0067_incremental_scope.py:111` `[arg-type]`) are pre-existing and unrelated, and the stale `[union-attr]` FR-7 named is gone.

**MINOR** · Python / Dimension 10 · `tests/test_0083_typed_helpers.py:37` <!-- harness-finding-key tests/test_0083_typed_helpers.py:37:MINOR:Python / Dimension 10 -->

The three builder tests take `module: object` and reach the helper through `getattr(module, "_f")`, which returns `Any` — so the pinning test that exists to prove type erasure was removed is itself written in an erasing form, and `getattr(module, "_f")(**overrides)` at line 74 is the very `**dict[str, object]` unpacking FR-4 eliminates, passing mypy only because `Any` absorbs it. Annotate the parameter `types.ModuleType` and access `module._f` directly rather than through a string literal.

**MINOR** · Core / Dimension 5 / Beck rule 3 · `tests/test_0083_typed_helpers.py:24` <!-- harness-finding-key tests/test_0083_typed_helpers.py:24:MINOR:Core / Dimension 5 / Beck rule 3 -->

`_FINDING_FIELDS` and `_FINDING_DEFAULTS` hardcode `Finding`'s field list and the builders' default values a third time, after `tests/test_0062_finding_key.py:11-15` and `tests/test_0067_incremental_scope.py:13-17`. Adding a sixth field to `gates/finding.py:Finding` will now fail `test_finding_builder_is_keyword_only_with_one_parameter_per_field` in a file that has nothing to do with the change. Derive the names from `dataclasses.fields(Finding)` so the test tracks the model instead of a copy of it.

**MINOR** · Core / Dimension 5 / Beck rule 3 · `tests/test_0062_finding_key.py:9` <!-- harness-finding-key tests/test_0062_finding_key.py:9:MINOR:Core / Dimension 5 / Beck rule 3 -->

This `_f` and `tests/test_0067_incremental_scope.py:11`'s are now identical line for line, docstring included, and `tests/test_0062_critic_reconciler.py:14` holds a third, positional `Finding` builder. The repo already supports the shared-sibling-helper pattern — `tests/_watch_fixtures.py` exists and `pyproject.toml:18-19` puts `tests` on `mypy_path` specifically so mypy can see such imports — so one `tests/_finding_builders.py` was available and would give the next `Finding` field change one place to edit instead of four. The per-file duplication follows `solution.md`'s Components table, so this is a design observation rather than a deviation.

**MINOR** · Testing / Dimension 22 · `tests/test_0083_mypy_config.py:79` <!-- harness-finding-key tests/test_0083_mypy_config.py:79:MINOR:Testing / Dimension 22 -->

`test_hooks_and_coverage_gate_carry_no_type_suppressions` asserts `"type: ignore" not in source` across the whole of three production files, which is both broader and narrower than the AC it cites. Broader: AC-4 says the two hook files are *otherwise unchanged*, not that they may never carry a suppression again, and `gates/coverage.py` is not named by AC-4 at all. Narrower: the substring misses `# type:ignore[...]` without the space, which mypy accepts. Assert against the specific lines the FRs are about, or delete the test and let the now-green `type_check` gate be the guard.

**MINOR** · Core / Dimension 6 / Information hiding · `tests/test_0083_mypy_config.py:53` <!-- harness-finding-key tests/test_0083_mypy_config.py:53:MINOR:Core / Dimension 6 / Information hiding -->

`assert len(overrides) == 1` plus `entry["module"] == ["sarif"]` pins the count of stubless third-party dependencies, not the property NFR-1 cares about. The exact-list match also forbids the conventional widening to `["sarif", "sarif.*"]`, which becomes necessary the moment a test writes `import sarif.loader` instead of `from sarif import loader`. Assert instead that every override's `module` entries come from an explicit allow-list of third-party distributions and that none names a first-party module; `test_no_blanket_ignore_missing_imports` at line 65 already carries the NFR-1 guarantee that matters.

**MINOR** · Core / Dimension 3 · `tests/test_0083_typed_helpers.py:42` <!-- harness-finding-key tests/test_0083_typed_helpers.py:42:MINOR:Core / Dimension 3 -->

New code uses single-letter and abbreviated bindings where the project's naming rule requires meaningful names for loop, comprehension, and lambda parameters: `p` here, `fn` at line 114, and `ln` at `tests/test_0083_mypy_config.py:75`. Related: the rewritten `load_baseline` call sites in `tests/test_regression_baseline_multilang.py` are inconsistent with themselves — lines 218 and 223 spell `lambda root, base`, while lines 243, 253, 265, and 272 keep `lambda r, b` and `lambda r, s, t`.

**OBS** · Python / Dimension 10 · `pyproject.toml:22` <!-- harness-finding-key pyproject.toml:22:OBS:Python / Dimension 10 -->

Adding `hooks` as a `mypy_path` root under `explicit_package_bases = true` changes how every `hooks/*.py` file is *named*, not just how its imports resolve: each is now qualified from the `hooks` root, so `hooks/_common.py` claims the top-level module name `_common`. No root-level or `tests/` module currently shares a basename with any of the six hooks files, which is why `mypy .` is clean. But `_common` is a generic name, and the day someone adds `tests/_common.py` two different files claim one module name and mypy fails the entire `type_check` gate with `Duplicate module named "_common"` — the class `solution.md`'s Risks section dismissed as inapplicable. Renaming `hooks/_common.py` is out of scope (AC-4 freezes the hooks), so note the constraint in the `mypy_path` comment.

**OBS** · Testing / Dimension 22 · `tests/test_0083_typed_helpers.py:1` <!-- harness-finding-key tests/test_0083_typed_helpers.py:1:OBS:Testing / Dimension 22 -->

This file is a test of other test files: it imports four sibling test modules and asserts on their private helpers' signatures, parameter kinds, and arity, and `test_capture_comments_returns_the_captured_review_comment_list` at line 80 drives `pr_commenter._submit_review` directly rather than through `post_findings`. The behavioural halves earn their place. The introspection halves duplicate what `mypy .` now enforces on every run, at the cost of real coupling: renaming `_f` in either builder file, or `_passing_gate_fns` in `tests/test_0036_parallel_gate.py`, breaks a third file with no runtime relationship to it. Worth trimming to the behavioural assertions once the gate is green and staying so.

**OBS** · Core / Dimension 6 · `.tickets/0083-clear-type-check-debt-for-coverage-gate/requirements.md:77` <!-- harness-finding-key .tickets/0083-clear-type-check-debt-for-coverage-gate/requirements.md:77:OBS:Core / Dimension 6 -->

The ticket clears its stated requirements but not its stated problem: `_append_coverage_gate` still does not fire, because `security`/bandit still fails with 13 findings that `problem.md` puts out of scope, so the coverage gate writes no `gate-findings.json` and `deliver-ticket.md` Step 1.6 will need the same manual sidecar workaround that `problem.md` cites as the motivation for the work. `requirements.md`'s Test Strategy pre-registered this, so nothing here is a contract breach — but the lead should expect the workaround at delivery and may want a follow-up ticket for the bandit findings (`memory.py:244` B608, plus B108 in `tests/test_0043_visible_gate_tool_skips.py` and `tests/test_context_rank.py`).
