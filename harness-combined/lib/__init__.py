"""The harness-combined implementation modules.

Every module the MCP server, the gate runner, the ticket ledger, and the
CLI entry points are built from lives here. The package is flat by design,
mirroring its sibling `gates` package: one directory per concern at the
plugin root, not a concern hierarchy inside each one.
"""
