mcp>=1.0
# Gate tooling — the directory-mode Python gates invoke these via
# `sys.executable -m <tool>`, so they must live in the server's own venv
# (the project under test may be stdlib-only and ship none of them).
ruff>=0.5
mypy>=1.10
pytest>=8.0
bandit>=1.7
textual>=0.50
# Coverage gate reads per-language thresholds from `.tickets/_thresholds.yaml`.
# Imported lazily by gates/coverage.py — only required when that file is present.
PyYAML>=6.0
